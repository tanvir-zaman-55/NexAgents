Create a new Chef project by opening http://127.0.0.1:5173 in the browser and starting a new chat.

Provide clear instructions to help the user:
1. The Chef UI is running at http://127.0.0.1:5173
2. Click "New Chat" to start a new project
3. Describe what you want to build in natural language
4. Chef will generate the full-stack app with Convex backend

Note: The user should already have `pnpm run dev` and `npx convex dev` running in separate terminals.
